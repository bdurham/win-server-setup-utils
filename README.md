# win-server-setup-utils

Windows command line utilities (*.cmd) to install and verify basic dev tools and utilities on a Windows server.

Used on the initial version of the Intrawest/Alterra project.
